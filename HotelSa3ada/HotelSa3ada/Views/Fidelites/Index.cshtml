@model IEnumerable<HotelSa3ada.Models.Fidelite>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Points)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Niveau)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdClientNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Points)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Niveau)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdClientNavigation.IdClient)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdFidelite">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdFidelite">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdFidelite">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
